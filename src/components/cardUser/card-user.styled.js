import styled from "styled-components";

export const StyledCardUser = styled.section`
.card-link {
        display: flex;
   
        &__text {
            font-weight: 700;
            font-size: 14px;
            line-height: 16px;
            color: #23262F;
            padding-left: 6px;
            text-decoration: none;
    }
}
`